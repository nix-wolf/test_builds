program network_test;

uses
  System.StartUpCopy,
  FMX.Forms,
  NetworkTest in 'NetworkTest.pas' {NetFrame},
  uNetworkManager in 'uNetworkManager.pas',
  uNetworkTypes in 'uNetworkTypes.pas';

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TNetFrame, NetFrame);
  Application.Run;
end.
