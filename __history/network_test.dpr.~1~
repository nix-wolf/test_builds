program network_test;

uses
  System.StartUpCopy,
  FMX.Forms,
  NetworkTest in 'NetworkTest.pas' {Form2};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
